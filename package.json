{
  "name": "nemolore",
  "version": "1.2.0",
  "description": "Memory management and automated lorebook creation toolkit for SillyTavern.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec tests/*.test.js",
    "test:smoke": "node --test --test-reporter=spec tests/import-smoke.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
